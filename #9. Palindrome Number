class Solution(object):
    def isPalindrome(self, x):
        if x <= 0:
            return False
        else:
            y = str(x)
            z = (y[::-1])
            if y == z:
                return True
            else:
                return False
